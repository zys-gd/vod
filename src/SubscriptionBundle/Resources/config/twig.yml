services:
  SubscriptionBundle\Twig\SubscriptionStatusExtension:
    tags:
      - { name: twig.extension }
    arguments:
      ['@session', '@IdentificationBundle\Repository\UserRepository', '@SubscriptionBundle\Repository\SubscriptionRepository']