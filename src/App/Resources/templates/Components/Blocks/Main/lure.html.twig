{% macro render(text) %}
    <div class="lure_box d-flex flex-column align-items-center justify-content-center vw-100">
        <div>
            <img class="img-fluid px-5" src="{{ asset('img/lp/lp_channels.png') }}" alt=""/>
        </div>
        <div class="lure_box_title mt-2">
            <span>{{ text }}</span>
        </div>
    </div>
{% endmacro render %}