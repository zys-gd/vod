{% extends '@App/base.html.twig' %}

{% import importTemplate("Common/Blocks/header.html.twig") as  header %}
{% import importTemplate("Components/Buttons/link.html.twig") as link %}

{% block header_content %}
    {{ header.render() }}
{% endblock header_content %}

{% block body %}
    <div class="d-flex align-items-center justify-content-center flex-column w-100">
        <div class=" contact_us__disclaimer mt-4">
            <div class=" text-center">
                <p>{{ translate('contact_us.thanks') }}</p>
            </div>
        </div>
        <div class="contact_us__disclaimer">
            <div class=" text-center">
                <p>{{ translate('contact_us.promise') }}</p>
            </div>
        </div>
        <div>
            {{ link.render(path('index'), translate('contact_us.link.text')) }}
        </div>
    </div>
{% endblock %}